// Operador Ternário

// ? = if | : = else

const idade = 2;

// idade >= 18 ? console.log('Você é maior de idade') : console.log('Você é menor de idade!'); // mais útil para retorna valores, qunado tem 2 condições

const msg = idade >= 18 ? 'Maior de idade' : 'Menor de idade';  
console.log(msg);

// Falsy = 0, undefined, '', NaN
// truthy = todos os demais valores

